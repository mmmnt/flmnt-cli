#!/usr/bin/env bash

echo "[Search recorded context first] Before grepping, Reading, or Exploring the codebase to answer this, FIRST search the connected MCP memory tools — especially flmnt (and the local quorum MCP). Use materialize_context for a recent snapshot, query_context for query-driven causal retrieval, and search_events / read_keyframe / slice_events / peek_stream / get_graph_neighborhood against the project's ::domain, ::plan, ::operational, ::mistake, and keyframe streams to see if the answer (a decision, root cause, runbook, or 'where X lives') is already recorded. That is the entire purpose of the tool and saves rediscovering it from source. Only fall back to code search for what the streams do not cover, or to verify a recalled fact still matches current code."
